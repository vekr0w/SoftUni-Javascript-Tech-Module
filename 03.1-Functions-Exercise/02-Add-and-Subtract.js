function calculate(numOne, numTwo, numThree) {

  let sumOfFirstTwo = Number(numOne) * Number(numTwo);
  let dividedSum = sumOfFirstTwo / Number(numThree);
  console.log(dividedSum);

}
calculate(23, 6, 10);

//not working